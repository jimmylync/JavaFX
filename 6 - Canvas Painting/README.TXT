Assignment:
Design and create an image using the Canvas class. Use at least one of each of the following:
rectangle shape, oval shape, polygon shape, text, color, gradient (linear or radial), effect.
